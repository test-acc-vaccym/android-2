<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.edroplet.qxx.saneteltabactivity.activities.ApplicationActivity">

    <android.support.design.widget.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/appbar_padding_top"
        android:theme="@style/AppTheme.AppBarOverlay">

        <include android:id="@+id/main_status_toolbar"
            layout="@layout/status_bar"/>
        <include android:id="@+id/main_operate_bar"
            layout="@layout/operate_bar"/>
        <android.support.v7.widget.Toolbar
            android:id="@+id/main_content_toolbar"
            android:layout_width="match_parent"
            app:titleTextAppearance="@dimen/status_bar_text_size"
            android:layout_height="@dimen/status_bar_height"
            android:background="?attr/colorPrimary"
            app:layout_scrollFlags="scroll|enterAlways"
            app:navigationIcon="@mipmap/ic_arrow_back_24dp"
            android:logo="@mipmap/ic_arrow_back_black_24dp"
            app:titleTextColor="@color/operate_confirm_message_color"
            app:popupTheme="@style/AppTheme.PopupOverlay">
        </android.support.v7.widget.Toolbar>
        <!--分割线-->
        <View
            android:layout_width="match_parent"
            android:layout_height="@dimen/toolbar_divider"
            android:background="@color/gray"/>
    </android.support.design.widget.AppBarLayout>
    <android.support.v4.view.ViewPager
        android:id="@+id/main_viewpager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <android.support.design.widget.BottomNavigationView
        android:id="@+id/main_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:menu="@menu/main_navigation"
        app:itemBackground="@color/bottom_tab_nav_background"
        app:itemTextColor="@color/tab_unchecked"/>
        <!--app:itemIconTint="@color/yellow"-->
        <!--android:background="?android:attr/windowBackground"-->

</LinearLayout>
